services:
  - type: web
    name: poe-chat
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: POE_API_KEY
        sync: false
